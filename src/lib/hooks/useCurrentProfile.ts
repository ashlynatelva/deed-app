"use client";

// Re-exports for ergonomic imports. The implementation lives next to the
// provider in `components/shared/CurrentProfileProvider`.
export { useCurrentProfile } from "@/components/shared/CurrentProfileProvider";
